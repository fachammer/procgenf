evaluationDependsOnChildren()

allprojects {
	apply plugin: 'eclipse'
}

subprojects {
  group = "fabianachammer"
  version = 0.1
  
  apply plugin: 'java'
  apply plugin: 'maven'
  
  repositories {
    mavenCentral()
  }
}
